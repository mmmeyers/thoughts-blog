<%= form_for(@affiliation) do |f| %>
  <% if @affiliation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliation.errors.count, "error") %> prohibited this affiliation from being saved:</h2>

      <ul>
      <% @affiliation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% if @affiliation.errors[:name].empty? %>
  <div class="field">
<% else %>
  <div class="field_with_errors">
<% end %>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

<% if @affiliation.errors[:description].empty? %>
  <div class="field">
<% else %>
  <div class="field_with_errors">
<% end %>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
